import React from "react";
import AddForm from "./AddForm";
import AddStyles from "./AddStyles";

const Add = (props) => {
  return (
    <AddStyles>
      <AddForm />
    </AddStyles>
  );
};

export default Add;
